# PR: fix-port-config-save

**Created:** 2026-03-30 15:50:54
**Branch:** pr/fix-port-config-save
**Worktree Location:** /Users/phaser/projects/shmoxy/../shmoxy-worktrees/fix-port-config-save

## Description

Fix port changes in config save being silently ignored (closes #112)

## Changes

1. **ProxyControlApi**: IPC PUT /ipc/config now updates ALL config fields (Port, CertPath, KeyPath, LogLevel, MaxConcurrentConnections, PassthroughHosts, EnabledDetectors, TempPassthroughMaxConnections, TempPassthroughTimeoutSeconds) instead of only 3 fields.
2. **ProxyProcessManager**: Added `RestartAsync(int? portOverride)` method and `_portOverride` field so port can be changed on restart.
3. **ConfigController**: Detects port changes by comparing current vs new config. On port change, restarts proxy with new port and re-applies full config after restart.
4. **IProxyProcessManager**: Added `RestartAsync` to interface.

## Notes

- Config re-application after restart is needed because the new proxy process starts with default config values. Full config persistence (#113) will eliminate this workaround.
- Non-port fields (LogLevel, PassthroughHosts, etc.) still apply at runtime without restart.

---
*This document was auto-generated by scripts/new-pr.sh*
