# PR: passthrough-detectors

**Created:** 2026-03-28 19:29:56
**Branch:** pr/passthrough-detectors
**Worktree Location:** /Users/phaser/projects/shmoxy/../shmoxy-worktrees/passthrough-detectors

## Description

Add a plugin system for auto-detecting domains that should bypass MITM via TLS passthrough.
Detectors analyze intercepted responses and suggest domains as passthrough candidates.
Users confirm or dismiss suggestions in the UI; confirmed domains are added to the passthrough list.

Closes #100

## Changes

- `IPassthroughDetector` interface with `DetectorContext` and `DetectorResult`
- Three detectors: Cloudflare Bot Detection, WAF Block, OAuth Token Endpoint
- `PassthroughDetectorHook` plugged into the intercept chain (tracks request/response pairs)
- Suggestion deduplication and dismissal tracking
- `EnabledDetectors` config in `ProxyConfig` with IPC API support
- IPC endpoints: list/enable/disable detectors, suggestion stream, accept/dismiss suggestions
- `DetectorsController` in shmoxy.api for frontend API layer
- Frontend UI: detector toggles and suggestion accept/dismiss cards
- 25 new unit tests for all three detectors and the hook

## Status

Complete - all tests passing, zero warnings, nix build succeeds.

---
*This document was auto-generated by scripts/new-pr.sh*
