# PR: persist-config

**Created:** 2026-03-30 17:09:20
**Branch:** pr/persist-config
**Worktree Location:** /Users/phaser/projects/shmoxy/../shmoxy-worktrees/persist-config

## Description

Persist proxy configuration to disk so it survives restarts (closes #113)

## Changes

1. **IConfigPersistence + JsonConfigPersistence**: New service that persists ProxyConfig to `~/Library/Application Support/shmoxy/proxy-config.json`
2. **ConfigController**: Persists config to disk after every successful IPC update
3. **ProxyProcessManager**: Loads persisted config on startup, uses persisted port, and re-applies full config via IPC after proxy becomes healthy
4. **DI registration**: Registered as singleton in Program.cs

## Notes

- Config file location defaults to `~/Library/Application Support/shmoxy/proxy-config.json`
- Uses `JsonStringEnumConverter` for human-readable enum values
- Gracefully handles missing or corrupt config files

---
*This document was auto-generated by scripts/new-pr.sh*
