# PR: remove-nix

**Created:** 2026-07-15 15:11:19
**Branch:** pr/remove-nix
**Worktree Location:** /Users/cristian.bidea/projects/shmoxy/../shmoxy-worktrees/remove-nix

## Description

Remove Nix build definition and all references (flake, docs, agent instructions)

## Notes

Add any relevant notes, decisions, or context here.

---
*This document was auto-generated by scripts/new-pr.sh*

## Changes

- Deleted `flake.nix`, `flake.lock`, `deps.nix`
- `README.md`: removed Nix prerequisite, "Using Nix Flakes", "Using Nix Build Directly", and "Development with Nix" sections
- `AGENTS.md`: removed the "Nix Build Verification" section (nix build is no longer a required gate)
- `.claude/agents/programmer.md`: removed the "Verify Nix build" workflow step
- `.claude/skills/work-issues/SKILL.md`: removed the `nix build .#shmoxy` verification gate
- `docs/scripts/convert-md.sh`: removed the nix-shell install hint
- `.gitignore`: removed the Nix artifacts block
- `ProxyProcessManager.cs`: reworded a comment that referenced nix build

Historical records (`docs/prs/*.md`) were left untouched.

## Verification

- `dotnet build` still succeeds (nix files were not part of the .NET build)
- No `nix` references remain outside git history and historical PR docs
